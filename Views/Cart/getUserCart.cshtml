@model Cart


@{
    ViewData["Title"] = "getUserCart";
}

<div style=" width:90%; margin:auto" class="mt-2"> 

    @if (Model != null && Model.cartDetails != null && Model.cartDetails.Count > 0)
    {


        <h3>My Cart</h3>



        <table class="table table-striped">
            <tr>
                <th>Product</th>
                <th>Image</th>
                <th>Unit Price(INT)</th>
                <th>Total Price</th>
                <th>Action</th>
            </tr>


            @foreach (var item in Model.cartDetails)

            {
                <tr>
                    <td>@item.Products.ProductName</td>
                    <td>
                        @if (string.IsNullOrEmpty(item.Products.Image))
                        {
                         <img src="/img/noimage.jpg" style="width:80px; height:100px"/>
                        }

                        else

                        {
                            <img src="/img/@item.Products.Image" style="width:80px; height:100px" />
                        }
                    </td>

                    <td>
                        @item.Products.Price X @item.Quantity
                    </td>

                    <td>
                        @(item.Products.Price*item.Quantity)
                    </td>

                    <td>
                        @if (item.Quantity < item.Products.Stocks.Quantity)
                        {

                            <a class="btn btn-info" href="/Cart/AddItem?productId=@item.ProductId&&redirect=1">+</a>

                        }

                        else
                        {
                            <span style="border: 1px solid;padding: 5px 8px; color:indianred;border-radius: 5px;"> OUT OF STOCK</span>
                        }


                        <a class="btn btn-info" href="/Cart/RemoveItem?productId=@item.ProductId">-</a>
                    </td>

                </tr>
            }
        </table>

        <div class="my-2">
            <h5>Total(INR): </h5>$ 
            @(Model.cartDetails.Select(item=>item.Products.Price*item.Quantity).Sum())
        </div>

        <div class="my-2">
            <a class="btn btn-primary" href="/Cart/Checkout"> Checkout </a>
        </div>

    }

    else
    {
        <h5>Cart is empty</h5>
    }




</div>

